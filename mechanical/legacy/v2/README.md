# V2 — SUPERSEDED

This is the archived side-lane prototype. The active design is ../../freecad/arm_0.FCStd.
Historical source/scripts and reports are snapshots; paths describe the original V2 layout, not a standalone build tree. To reproduce V2 exactly use Git commit fff31ea in a separate checkout. Do not use its poses or validation results for V3.
